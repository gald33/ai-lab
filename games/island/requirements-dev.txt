# What the tests need on top of `requirements.txt`.
#
# They stand a **real hub** on a loopback port rather than mocking the client,
# so the thing under test is the thing that runs -- and that means the server
# half of Switchboard, which a host never installs because it never serves.
-r requirements.txt
agent-switchboard[server]>=2.0.1
pytest

# `tools/tests/test_workflows.py` reads the workflow files as YAML rather than
# grepping them: a `paths:` filter is a nested structure, and a regex over it
# would pass on a filter that parses to something else entirely.
pyyaml
