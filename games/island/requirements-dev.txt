# What the tests need on top of `requirements.txt`.
#
# They stand a **real hub** on a loopback port rather than mocking the client,
# so the thing under test is the thing that runs -- and that means the server
# half of Switchboard, which a host never installs because it never serves.
-r requirements.txt
agent-switchboard[server]>=1.2.2
pytest
